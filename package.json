{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    // ... suas dependências
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "CI=false react-scripts build"  // <<<< CORREÇÃO AQUI
  },
  "scripts": {
  "start": "react-scripts start",
  "build": "npx react-scripts build"
},
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}